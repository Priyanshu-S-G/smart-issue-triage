# Smart Issue Triage does not require any API keys or external services.
# This file is kept for compatibility with the assessment's required structure
# and to document how you would wire up an optional real LLM provider later.

# Optional: only needed if you swap the local scoring engine for a real LLM call.
# OPENAI_API_KEY=
# GEMINI_API_KEY=
